import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#121214',
    padding: 42,
  },
  titulo: {
    color: '#FFF',
    fontSize: 32,
    marginTop: 32,
    textAlign: 'center',
  }
});